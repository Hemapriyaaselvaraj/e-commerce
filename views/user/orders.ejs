<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Orders - ToughToes</title>
  <link rel="stylesheet" href="/css/profile.css" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  <link rel="stylesheet" href="/css/common.css" />
  <link rel="stylesheet" href="/css/footer.css" />
  
  <style>
    /* Keep original background and basic styling */
    body {
      padding-top: 0;
    }

    /* Navbar styles - matching profile page */
    .navbar {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      z-index: 1000;
      background: #fff;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 1rem 1.5rem;
      border-bottom: 1px solid #ddd;
    }

    .nav-links a {
      margin: 0 1rem;
      text-decoration: none;
      color: #000;
    }

    .nav-right {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-left: auto;
      margin-right: 1.5rem;
    }

    .search-input {
      padding: 0.5rem;
      border: 1px solid #ccc;
      min-width: 180px;
    }

    /* Sidebar styles - matching profile page */
    .sidebar {
      width: 200px;
      background: #f8f9fa;
    }

    .sidebar ul li {
      margin-bottom: 10px;
    }

    .sidebar a {
      display: block;
      padding: 8px 15px;
      text-decoration: none;
      color: #333;
      border-radius: 5px;
      transition: all 0.3s ease;
    }

    .sidebar a.active,
    .sidebar a:hover {
      background-color: #f8f9fa;
      font-weight: 500;
    }

    .sidebar a.active {
      background-color: #f8f9fa;
      border-radius: 5px;
      font-weight: 500;
    }

    #logoutForm button {
      width: 100%;
      display: flex;
      align-items: center;
      font-size: inherit;
      font-family: inherit;
      padding: 8px 15px;
      cursor: pointer;
      color: #333;
      background: none;
      border: none;
      text-align: left;
      border-radius: 5px;
      transition: all 0.3s ease;
    }

    #logoutForm button:hover {
      background-color: #f8f9fa;
    }

    /* Badges with gradient styling */
    .badge {
      padding: 0.5rem 1rem;
      border-radius: 50px;
      font-weight: 600;
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .bg-success {
      background: linear-gradient(135deg, #48bb78, #38a169) !important;
      color: white !important;
    }

    .bg-primary {
      background: linear-gradient(135deg, #667eea, #764ba2) !important;
      color: white !important;
    }

    /* Buttons with gradient theme */
    .btn {
      border-radius: 12px;
      font-weight: 600;
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
      border: none;
    }

    .btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
      transition: left 0.5s;
    }

    .btn:hover::before {
      left: 100%;
    }

    .btn:hover {
      transform: translateY(-2px);
    }

    .btn-outline-primary {
      background: rgba(102, 126, 234, 0.1);
      border: 2px solid rgba(102, 126, 234, 0.3);
      color: #667eea;
    }

    .btn-outline-primary:hover {
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
      border-color: #667eea;
      box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
    }

    .btn-outline-secondary {
      background: rgba(102, 126, 234, 0.05);
      border: 2px solid rgba(102, 126, 234, 0.2);
      color: #667eea;
    }

    .btn-outline-secondary:hover {
      background: linear-gradient(135deg, #764ba2, #667eea);
      color: white;
      border-color: #764ba2;
      box-shadow: 0 8px 25px rgba(118, 75, 162, 0.3);
    }

    .btn-dark {
      background: linear-gradient(135deg, #667eea, #764ba2);
      color: white;
    }

    .btn-dark:hover {
      background: linear-gradient(135deg, #764ba2, #667eea);
      box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
    }

    .btn-sm {
      padding: 0.5rem 1rem;
      font-size: 0.875rem;
      border-radius: 10px;
    }

    /* Pagination with black and white theme */
    .pagination {
      gap: 4px;
    }

    .pagination .page-item {
      margin: 0;
    }

    .pagination .page-link {
      font-size: 14px;
      padding: 8px 12px;
      border: 1px solid #dee2e6;
      background: #fff;
      color: #6c757d;
      border-radius: 4px;
      transition: all 0.2s ease;
      font-weight: normal;
    }

    .pagination .page-item.active .page-link {
      background: #212529;
      color: white;
      border-color: #212529;
    }

    .pagination .page-item.disabled .page-link {
      background: #fff;
      color: #6c757d;
      cursor: not-allowed;
      border-color: #dee2e6;
      opacity: 0.5;
    }

    .pagination .page-link:hover:not(.disabled) {
      background: #e9ecef;
      border-color: #adb5bd;
      color: #495057;
    }

    .pagination .page-item.active .page-link:hover {
      background: #212529;
      border-color: #212529;
      color: white;
    }

    /* Responsive adjustments */
    @media (max-width: 768px) {
      body {
        padding-top: 80px;
      }

      .container.d-flex {
        flex-direction: column;
        padding: 0 15px;
      }

      .sidebar {
        width: 100%;
        margin-bottom: 20px;
      }
    }
  </style>
</head>
<body>
  <%- include('../partials/userNavbar', { 
    name: name, 
    cartCount: 0, 
    wishlistCount: 0 
  }) %>

  <div class="container my-5 d-flex" style="margin-top: 90px !important; padding-top: 0;">
    
    <div class="sidebar p-3 me-4 border rounded">
      <ul class="list-unstyled">
        <li><a href="/profile"><i class="fas fa-user me-2"></i>Profile</a></li>
        <li><a href="/order/my-orders" class="active"><i class="fas fa-box me-2"></i>Orders</a></li>
        <li><a href="/wishlist"><i class="fas fa-heart me-2"></i>Wishlist</a></li>
        <li><a href="/addresses"><i class="fas fa-map-marker-alt me-2"></i>Addresses</a></li>
        <li><a href="/wallet"><i class="fas fa-wallet me-2"></i>My Wallet</a></li>
        <li><a href="/profile/referAndEarn"><i class="fas fa-users me-2"></i>Refer & Earn</a></li>

        <li>
          <form id="logoutForm" method="POST" action="/user/logout">
            <button type="submit">
              <i class="fas fa-sign-out-alt me-2"></i>Logout
            </button>
          </form>
        </li>
      </ul>
    </div>

    
    <div class="flex-grow-1">
      <div class="profile-box border rounded p-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
          <h4>My Orders <small class="text-muted">(<%= totalOrders %> total)</small></h4>
          <form method="GET" action="/order/my-orders" class="d-flex gap-2">
            <input type="text" class="form-control form-control-sm" name="search" placeholder="Search order number..." style="width: 200px;" value="<%= search %>">
            <select class="form-select form-select-sm" name="status" style="width: 150px;">
              <option value="">All Status</option>
              <option value="ORDERED" <%= status === 'ORDERED' ? 'selected' : '' %>>Ordered</option>
              <option value="SHIPPED" <%= status === 'SHIPPED' ? 'selected' : '' %>>Shipped</option>
              <option value="OUT_FOR_DELIVERY" <%= status === 'OUT_FOR_DELIVERY' ? 'selected' : '' %>>Out for Delivery</option>
              <option value="DELIVERED" <%= status === 'DELIVERED' ? 'selected' : '' %>>Delivered</option>
              <option value="RETURNED" <%= status === 'RETURNED' ? 'selected' : '' %>>Returned</option>
              <option value="CANCELLED" <%= status === 'CANCELLED' ? 'selected' : '' %>>Cancelled</option>
            </select>
            <select class="form-select form-select-sm" name="dateFilter" style="width: 150px;">
              <option value="">All Time</option>
              <option value="30" <%= dateFilter === '30' ? 'selected' : '' %>>Last 30 Days</option>
              <option value="90" <%= dateFilter === '90' ? 'selected' : '' %>>Last 3 Months</option>
              <option value="180" <%= dateFilter === '180' ? 'selected' : '' %>>Last 6 Months</option>
              <option value="365" <%= dateFilter === '365' ? 'selected' : '' %>>Last Year</option>
            </select>
            <button type="submit" class="btn btn-outline-primary btn-sm">
              <i class="fas fa-search"></i>
            </button>
            <a href="/order/my-orders" class="btn btn-outline-secondary btn-sm">
              <i class="fas fa-times"></i>
            </a>
          </form>
        </div>
        <% if (orders && orders.length > 0) { %>
          <div class="orders-list">
            <% orders.forEach(function(order) { %>
              <div class="order-item card mb-3">
                <div class="card-body">
                  <div class="d-flex justify-content-between align-items-center mb-2">
                    <h5 class="card-title mb-0">Order #<%= order.orderNumber %></h5>
                    <span class="badge bg-<%= order.status === 'DELIVERED' ? 'success' : 'primary' %>">
                      <%= order.status %>
                    </span>
                  </div>
                  <p class="card-text">
                    <small class="text-muted">Ordered on <%= new Date(order.createdAt).toLocaleDateString() %></small>
                  </p>
                  <div class="order-items">
                    <% order.items.forEach(function(item) { %>
                      <div class="order-product d-flex align-items-center mb-2">
                        <img src="<%= item.image %>" alt="<%= item.name %>" class="me-3" style="width: 60px; height: 60px; object-fit: cover;">
                        <div>
                          <h6 class="mb-0"><%= item.name %></h6>
                          <small>Size: <%= item.size %>, Color: <%= item.color %></small>
                          <p class="mb-0">₹<%= item.price %> × <%= item.quantity %></p>
                        </div>
                      </div>
                    <% }); %>
                  </div>
                  <div class="order-total mt-3 pt-2 border-top d-flex justify-content-between align-items-center">
                    <strong>Total: ₹<%= order.totalAmount %></strong>
                    <a href="/order/details/<%= order._id %>" class="btn btn-outline-primary btn-sm">
                      <i class="fas fa-eye me-1"></i> View Details
                    </a>
                  </div>
                </div>
              </div>
            <% }); %>
          </div>
          <!-- Pagination -->
          <% if (totalPages > 1) { %>
            <div class="d-flex justify-content-between align-items-center mt-4">
              <div class="text-muted" style="font-size: 14px;">
                Showing <%= (currentPage - 1) * 5 + 1 %> to <%= Math.min(currentPage * 5, totalOrders) %> of <%= totalOrders %> orders
              </div>
              
              <nav>
                <ul class="pagination mb-0">
                  <!-- Previous Page -->
                  <li class="page-item <%= !hasPrevPage ? 'disabled' : '' %>">
                    <% if (hasPrevPage) { %>
                      <a class="page-link" href="?page=<%= prevPage %><%= search ? '&search=' + encodeURIComponent(search) : '' %><%= status ? '&status=' + status : '' %><%= dateFilter ? '&dateFilter=' + dateFilter : '' %>">Previous</a>
                    <% } else { %>
                      <span class="page-link">Previous</span>
                    <% } %>
                  </li>

                  <!-- Page Numbers -->
                  <% 
                    let startPage = Math.max(1, currentPage - 2);
                    let endPage = Math.min(totalPages, currentPage + 2);
                    
                    if (currentPage <= 3) {
                      endPage = Math.min(5, totalPages);
                    }
                    if (currentPage > totalPages - 3) {
                      startPage = Math.max(1, totalPages - 4);
                    }
                  %>

                  <% if (startPage > 1) { %>
                    <li class="page-item">
                      <a class="page-link" href="?page=1<%= search ? '&search=' + encodeURIComponent(search) : '' %><%= status ? '&status=' + status : '' %><%= dateFilter ? '&dateFilter=' + dateFilter : '' %>">1</a>
                    </li>
                    <% if (startPage > 2) { %>
                      <li class="page-item disabled">
                        <span class="page-link">...</span>
                      </li>
                    <% } %>
                  <% } %>

                  <% for (let i = startPage; i <= endPage; i++) { %>
                    <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                      <% if (i === currentPage) { %>
                        <span class="page-link"><%= i %></span>
                      <% } else { %>
                        <a class="page-link" href="?page=<%= i %><%= search ? '&search=' + encodeURIComponent(search) : '' %><%= status ? '&status=' + status : '' %><%= dateFilter ? '&dateFilter=' + dateFilter : '' %>"><%= i %></a>
                      <% } %>
                    </li>
                  <% } %>

                  <% if (endPage < totalPages) { %>
                    <% if (endPage < totalPages - 1) { %>
                      <li class="page-item disabled">
                        <span class="page-link">...</span>
                      </li>
                    <% } %>
                    <li class="page-item">
                      <a class="page-link" href="?page=<%= totalPages %><%= search ? '&search=' + encodeURIComponent(search) : '' %><%= status ? '&status=' + status : '' %><%= dateFilter ? '&dateFilter=' + dateFilter : '' %>"><%= totalPages %></a>
                    </li>
                  <% } %>

                  <!-- Next Page -->
                  <li class="page-item <%= !hasNextPage ? 'disabled' : '' %>">
                    <% if (hasNextPage) { %>
                      <a class="page-link" href="?page=<%= nextPage %><%= search ? '&search=' + encodeURIComponent(search) : '' %><%= status ? '&status=' + status : '' %><%= dateFilter ? '&dateFilter=' + dateFilter : '' %>">Next</a>
                    <% } else { %>
                      <span class="page-link">Next</span>
                    <% } %>
                  </li>
                </ul>
              </nav>
            </div>
          <% } %>
        <% } else { %>
          <div class="text-center py-5">
            <i class="fas fa-box-open fa-3x mb-3 text-muted"></i>
            <p class="text-muted">No orders found.</p>
            <a href="/" class="btn btn-dark">Start Shopping</a>
          </div>
        <% } %>
      </div>
    </div>
  </div>
    <%- include('../partials/footer') %>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    // Auto-submit form when filters change
    document.addEventListener('DOMContentLoaded', function() {
      const form = document.querySelector('form[action="/order/my-orders"]');
      
      if (!form) {
        console.error('Form not found!');
        return;
      }
      
      console.log('Form found:', form);
      
      const selects = form.querySelectorAll('select');
      const searchInput = form.querySelector('input[name="search"]');
      
      console.log('Found selects:', selects.length);
      console.log('Found search input:', searchInput);
      
      selects.forEach((select, index) => {
        select.addEventListener('change', function() {
          console.log(`Select ${index} changed to:`, this.value);
          form.submit();
        });
      });

      // Submit form on Enter key in search input
      if (searchInput) {
        searchInput.addEventListener('keypress', function(e) {
          if (e.key === 'Enter') {
            e.preventDefault();
            console.log('Enter pressed, submitting form with search:', this.value);
            form.submit();
          }
        });
      }

      // Add click handler for search button
      const searchButton = form.querySelector('button[type="submit"]');
      if (searchButton) {
        searchButton.addEventListener('click', function(e) {
          e.preventDefault();
          console.log('Search button clicked');
          form.submit();
        });
      }
    });
  </script>
</body>
</html>
